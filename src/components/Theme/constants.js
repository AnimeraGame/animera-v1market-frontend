const appBarHeight = 92
const mobileAppBarHeight = 72
const drawerWidth = 260
const leftNavDrawerWidth = 275
const tabletWidth = 1024
const maxWidth = 1200
const mobileBreakpoint = 1020
const mobileBreakpointMin = 320
const smallScreenWidth = 1200
const minimumSupportRes = 720

const responsiveGrid = {
  sm: 6,
  md: 6,
  lg: 4,
  xl: 4,
  xs: 12,
}

export const constants = {
  appBarHeight,
  mobileAppBarHeight,
  drawerWidth,
  tabletWidth,
  leftNavDrawerWidth,
  maxWidth,
  responsiveGrid,
  mobileBreakpoint,
  mobileBreakpointMin,
  smallScreenWidth,
  minimumSupportRes,
}
